import Icon from '../src/components/Icon.jsx';

const AccountBookFilled = (props, context) => {
  // const p = { ...props, ...context.attrs };
  return <Icon ></Icon>;
};

export default AccountBookFilled;